const gridTemplateColumns = [''];

const gridAutoColumns = [''];

const gridColumn = [''];

const gridColumnStart = [''];

const gridColumnEnd = [''];

const gridTemplateRows = [''];

const gridAutoRows = [''];

const gridRow = [''];

const gridRowStart = [''];

const gridRowEnd = [''];

const gap = [''];

const gridAutoFlow = [
  'grid-flow-row',
  'grid-flow-col',
  'grid-flow-row-dense',
  'grid-flow-col-dense',
];

export const Grid = {
  gridTemplateColumns,
  gridAutoColumns,
  gridColumn,
  gridColumnStart,
  gridColumnEnd,
  gridTemplateRows,
  gridAutoRows,
  gridRow,
  gridRowStart,
  gridRowEnd,
  gap,
  gridAutoFlow,
};
