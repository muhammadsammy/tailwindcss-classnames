const width = [''];

const minWidth = [''];

const maxWidth = [''];

const height = [''];

const minHeight = [''];

const maxHeight = [''];

export const Sizing = {
  width,
  minWidth,
  maxWidth,
  height,
  minHeight,
  maxHeight,
};
