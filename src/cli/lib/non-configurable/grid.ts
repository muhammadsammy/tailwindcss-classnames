import {Grid} from '../types/classes';

const gridAutoFlow = [
  'grid-flow-row',
  'grid-flow-col',
  'grid-flow-row-dense',
  'grid-flow-col-dense',
];

const grid: Partial<Grid> = {
  gridAutoFlow,
};

export default grid;
