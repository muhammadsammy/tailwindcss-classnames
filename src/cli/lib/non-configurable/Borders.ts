const borderColor = [''];

const borderOpacity = [''];

const borderStyle = [
  'border-solid',
  'border-dashed',
  'border-dotted',
  'border-double',
  'border-none',
];

const borderWidth = [''];

const borderRadius = [''];

const divideWidth = [''];

const divideOpacity = [''];

const divideColor = [''];

const divideStyle = [
  'divide-solid',
  'divide-dashed',
  'divide-dotted',
  'divide-double',
  'divide-none',
];

export const Borders = {
  borderColor,
  borderOpacity,
  borderStyle,
  borderWidth,
  borderRadius,
  divideWidth,
  divideOpacity,
  divideColor,
  divideStyle,
};
