name: Lint & Build
on: [push, pull_request]

jobs:
  commit_&_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v1
        with:
          node-version: '14.x'
      - run: yarn install
      - name: lint
        run: npm run lint
      - name: build
        run: npm run build
